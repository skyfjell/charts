{{ if .Values.apps.externalDns.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.apps.externalDns.namespace }}
  labels:
    {{- include "platformSystem.labels" . | nindent 4 }}
    {{- if or (and .Values.apps.istio.enabled .Values.apps.externalDns.istioEnabled) (and .Values.apps.istio.enabled .Values.global.istioEnabled) }}
    istio-injection: enabled
    {{- end }}
{{ end }}
