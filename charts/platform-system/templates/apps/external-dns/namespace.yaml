{{ if .Values.components.externalDns.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.components.externalDns.namespace }}
  labels:
    {{- include "platform-system.labels" . | nindent 4 }}
    {{- if or (and .Values.components.istio.enabled .Values.components.externalDns.istioEnabled) (and .Values.components.istio.enabled .Values.global.istioEnabled) }}
    istio-injection: enabled
    {{- end }}
{{ end }}
