{{ if .Values.components.istio.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.components.istio.namespace }}
  labels:
    {{- include "platformSystem.labels" . | nindent 4 }}
    {{- if or (and .Values.components.istio.enabled .Values.components.istio.istioEnabled) (and .Values.components.istio.enabled .Values.global.istioEnabled) }}
    istio-injection: enabled
    {{- end }}
{{ end }}
