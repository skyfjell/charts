{{ if .Values.apps.vaultOperator.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.apps.vaultOperator.namespace }}
  labels:
    {{- include "platform-system.labels" . | nindent 4 }}
    {{- if or (and .Values.apps.istio.enabled .Values.apps.vaultOperator.istioEnabled) (and .Values.apps.istio.enabled .Values.global.istioEnabled) }}
    istio-injection: enabled
    {{- end }}
{{ end }}
