{{ if .Values.components.minioTenant.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.components.minioTenant.namespace }}
  labels:
    {{- include "platform-system.labels" . | nindent 4 }}
    {{- if or (and .Values.components.istio.enabled .Values.components.minioTenant.istioEnabled) (and .Values.components.istio.enabled .Values.global.istioEnabled) }}
    istio-injection: enabled
    {{- end }}
{{ end }}
