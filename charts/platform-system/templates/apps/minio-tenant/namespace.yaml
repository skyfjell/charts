{{ if .Values.apps.minioTenant.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.apps.minioTenant.namespace }}
  labels:
    {{- include "platform-system.labels" . | nindent 4 }}
    {{- if or (and .Values.apps.istio.enabled .Values.apps.minioTenant.istioEnabled) (and .Values.apps.istio.enabled .Values.global.istioEnabled) }}
    istio-injection: enabled
    {{- end }}
{{ end }}
