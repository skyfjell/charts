{{ if .Values.components.certManager.enabled }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Values.components.certManager.namespace }}
  labels: 
    {{- include "platformSystem.labels" . | nindent 4 }}
    {{- if or (and .Values.components.istio.enabled .Values.components.certManager.istioEnabled) (and .Values.components.istio.enabled .Values.global.istioEnabled) }}
    istio-injection: enabled
    {{- end }}
{{ end }}
