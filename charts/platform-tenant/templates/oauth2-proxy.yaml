{{- if .Values.appAuth.proxy.enabed -}}
{{- if .Values.crdCheck }}
  {{- include "require.api.flux.helmRelease" . -}}
{{- end -}}
---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: {{ include "platform-system.helper.proxyName" $ }}
  namespace: {{ $.Values.targetNamespace.name }}
spec:
  chart:
    spec:
      chart: {{ .Values.appAuth.proxy.chart.name }}
      version: {{ .Values.appAuth.proxy.chart.version }}
      sourceRef:
        kind: HelmRepository
        name: {{ include "platform-system.helper.proxyName" $ }}
        namespace: {{ .Values.flux.namespace }}
  interval: 1h0m0s
  values:
{{- $defaultValues := ( include "platform-tenant.oauth2-proxy.template" $ | fromYaml ) }}
{{ ( mergeOverwrite $defaultValues (default dict .Values.appAuth.proxy.overrideValues )) | toYaml | indent 4 }}
{{- end -}}