# {{- if .Values.components.rbac.enabled -}}
# kind: RoleBinding
# apiVersion: rbac.authorization.k8s.io/v1
# metadata:
#   namespace: {{ list . | include "platform-tenant.format.name.shared" }}
#   name: {{ list "deployer" . | include "platform-tenant.format.name.shared" }}
# subjects:
#   - kind: ServiceAccount
#     name: {{ default (list "deployer" . | include "platform-tenant.format.name.shared") .Values.components.serviceAccount.name }}
#     namespace: {{ .Values.components.flux.namespace }}
# roleRef:
#   apiGroup: rbac.authorization.k8s.io
#   kind: Role
#   name: {{ list "deployer" . | include "platform-tenant.format.name.shared" }}
# {{- end -}}
