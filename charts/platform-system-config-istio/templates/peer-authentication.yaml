{{ if .Values.apps.istio.enabled }}
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: "default"
  namespace: {{ .Values.solution.namespace | quote }}
spec:
  mtls:
    mode: {{ .Values.apps.istio.mTLSMode | default "STRICT" | quote }}
{{- end }}
