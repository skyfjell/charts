{{- if .Values.components.keycloak.enabled }}
  {{- if .Values.crdCheck }}
    {{- include "require.api.flux.source" . -}}
  {{- end -}}
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: HelmRepository
metadata:
  name: {{ include "platform-auth.helper.codecentricName" $ }}
  namespace: {{ .Values.components.flux.namespace }}
spec:
  url: {{ .Values.components.keycloak.chart.url }}
  interval: 10m
{{- end }}