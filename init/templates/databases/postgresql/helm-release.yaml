{{- if .Values.databases.postgresql.enabled  }}
{{- $tolerations := ( not .Values.databases.postgresql.tolerations | ternary $.Values.defaultTolerations.apps .Values.databases.postgresql.tolerations ) }}
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: system-auth-postgresql
  namespace: flux-system
spec:
  releaseName: system-auth-postgresql
  chart:
    spec:
      chart: postgresql
      sourceRef:
        kind: HelmRepository
        name: bitnami
  interval: 1h0m0s
  targetNamespace: system-auth
  values:
    auth:
      createAdminUser: false
    proxyAddressForwarding: true
    tolerations: {{ toYaml $tolerations | nindent 6 }}
    existingSecret: system-auth-postgresql
{{- end }}
